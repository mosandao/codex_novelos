## parent 判定：单原型

唯一选中原型直接为 parent，无辅助原型。

- `parent_rationale` 仍须输出：为什么这个原型承载 project_setup 的主基调（题材/情绪基调/美学的契合点在哪），以及错配警告（如有——教育/视野与情绪基调根本冲突时报告 mismatch 并给调和建议，不静默硬融）。
- 七字段以 parent 骨架为唯一血缘来源，禁止从全库清单里的其他原型取材。
