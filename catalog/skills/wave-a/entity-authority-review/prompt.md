# 实体权威来源审查

只审查实体提交/修订内容与已知 Canon 及权威历史快照的一致性。

检查实体属性变化、命名、制度规范、势力范围及规则设定是否违反既有 Canon 或产生未授权矛盾。每个问题使用 `blocking`、`warning` 或 `note`，引用最小片段和来源 ref。存在 `blocking` 时 verdict 必须为 `rejected`。

返回同一 `subject_hash`、verdict、findings、evidence refs 和 reviewer profile。
