# 连续性事实审查

只审查从章节提取的连续性候选与已接受正文及现有 Canon 的匹配度。

检查提取的事实变更、人物关系变化、事件状态、伏笔更新与正文 Hash 对应关系是否准确无误。每个问题使用 `blocking`、`warning` 或 `note`，引用最小片段和来源 ref。存在 `blocking` 时 verdict 必须为 `rejected`。

返回同一 `subject_hash`、verdict、findings、evidence refs 和 reviewer profile。
