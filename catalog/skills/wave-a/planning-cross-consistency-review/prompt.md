# 规划交叉一致性审查

只审查人物契约与世界契约交叉关系，不直接重写资产。

检查人物能力成长上限是否违反世界规则、势力和资源分配与人物背景/动机是否冲突、角色与制度机制的交互是否自洽。每个问题使用 `blocking`、`warning` 或 `note`，引用最小片段和来源 ref。存在 `blocking` 时 verdict 必须为 `rejected`。

返回同一 `subject_hash`、verdict、findings、evidence refs 和 reviewer profile。
