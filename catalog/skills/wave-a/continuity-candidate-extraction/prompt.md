# 连续性候选提取

只从已接受章节中提取正文明确发生或确认、且后续可能依赖的变化。不要把推测、氛围、建议或下章计划写成 Canon。

候选类型仅限 `fact`、`narrative_promise`、`expectation`、`relationship` 和 `arc`。每项必须包含该类型规定的非空字段，并可追溯到当前章节 `subject_hash`。宁缺毋滥；相同变化合并，不重复记录。

发现正文与权威上下文冲突时返回冲突 finding，不自行决定哪一方覆盖另一方。
