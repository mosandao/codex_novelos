# 底层规则体系设计方法

本方法用于定义故事世界的不可破底层法则与规则边界。

## 输入约束
- 必须基于已锁定的 Story Direction 与 Story Architecture。
- 不得凭空生成与主角既有背景或终局承诺矛盾的特权法则。

## 执行步骤
1. 定义底层法则的物理/超自然边界与不可违背的铁律。
2. 明确规则对所有角色（包括主角与反派）的无差别约束。
3. 设定规则生效的限制条件、失败残留效应与观察信号。

## 输出要求
生成 Markdown 格式的世界规则契约文档，包含法则描述、边界约束与代价残留。
