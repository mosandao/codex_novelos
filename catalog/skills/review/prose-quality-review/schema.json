{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["subject_hash", "verdict", "findings", "evidence_refs", "reviewer_profile"],
  "properties": {
    "subject_hash": {"type": "string", "pattern": "^sha256:[0-9a-f]{64}$"},
    "verdict": {"enum": ["approved", "rejected"]},
    "findings": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["severity", "message", "evidence_refs"],
        "properties": {
          "severity": {"enum": ["blocking", "warning", "note", "strength"]},
          "code": {"type": "string", "minLength": 1},
          "message": {"type": "string", "minLength": 1},
          "excerpt": {"type": "string"},
          "evidence_refs": {
            "type": "array",
            "items": {"type": "string", "minLength": 1},
            "uniqueItems": true
          },
          "defer_to_downstream": {
            "type": "string",
            "minLength": 1,
            "description": "下游豁免：warning 属下游执行责任时标注下游 asset_type"
          },
          "accepted_risk": {
            "type": "boolean",
            "description": "艺术风险豁免：warning 为有意创作取舍，经用户确认后豁免"
          },
          "accepted_by": {
            "type": "string",
            "minLength": 1,
            "description": "accepted_risk 的确认方（user）"
          }
        }
      }
    },
    "evidence_refs": {
      "type": "array",
      "items": {"type": "string", "minLength": 1},
      "uniqueItems": true
    },
    "reviewer_profile": {"type": "string", "minLength": 1, "description": "审查者模型身份（防共谋机器留痕）：model:<provider:model>（异构厂商模型直审）或 agent:<name>@<model>（具名审查 agent）；裸字符串在落库门不合法"}
  }
}
