# R1-G5 红方复审

> 审查人：G5 异构红方复审员（对抗审查，只读仓库 + 只写 /tmp 与本文件）。
> 审查对象：`git show ceec226` 全量 diff（7 文件，+405/−39）+ 盲测双向判据实测。
> 方法：任务一 = 本红方亲自扮演修订 agent，按新 prose-revision 模式A（指纹清除）对 fixtures A 组三段执行修订，机器计分（`scripts/novelos-prose-fingerprint.mjs --json`）+ 语义项逐条指认；B 组按 fixtures 登记段落范围切出实测。任务二 = diff 逐文件规格符合性审查 + 四命令重跑。
> 中间产物（全部在 /tmp/r1-blind/，未入仓库）：A1/A2/A3 原文与 `-revised`、B1/B2/B3 原文与 `-revised`、前后 `.json`、compose 产物两份。
> 结论预告：**修改后放行**，P0×0 / P1×2 / P2×8。

---

## 1. 盲测双向判据表（机器数字为主，主判据）

### 1.1 A 组 screen 层命中变化（`fingerprint --json` rules[].count 实测）

| 规则 | tier | FIX-R1-A1(851字) | FIX-R1-A2(1047字) | FIX-R1-A3(930字) | 合计 before → after |
|---|---|---|---|---|---|
| L01 翻案腔(窄) | screen | 4 → **0** | 0 | 0 | 4 → 0 |
| L03 破折号 | screen | 4 → 1 | 2 → 1 | 3 → **0** | 9 → 2 |
| L08 当…时 | screen | 0 | 0 | 1 → **0** | 1 → 0 |
| L11 这意味着 | screen | 1 → 1 | 0 | 8 → 6 | 9 → 7 |
| P01 相邻句同构 | screen | 2 → **3** ⚠ | 1 → **0** | 6 → 2 | 9 → 5 |
| P02 连续3句同构 | screen | 0 | 0 | 3 → 1 | 3 → 1 |
| P03 段首零回指 | screen | 0 | 0 | 4 → 3 | 4 → 3 |
| L02 顿号罗列 | screen | 0 | 0 | 0 | 0 → 0 |
| **screen 合计** | | **11 → 5** | **3 → 1** | **25 → 12** | **39 → 18（−54%）** |

measure 层（只测不判，记录项）：A1 B02 4→0、L01d 1→0；A3 T05 1→0；A2 M02 11→10（句内排比按 N7 保留——预期行为）；修复涟漪：A1 M03/M06 各 +1（改写句引入「就是」）、A3 B01 0→1（删「当…时」使「第二次查库」成段首）、A2 L03 残留 1 处为修订 agent 首轮遗漏「回不去了——」（自检未抓住，二次修补后计入上表）。

**残留归因（逐条）**：
- L03×2 残留：A1「他更亢奋——」/A2「名字——」均为展开/补充型非揭晓式，按 FP-1.7 逐处认语义**不命中，合法保留**。
- L11×7 残留：A3 的 6 处「这说明/这意味着」+A1 的 1 处，经 FP-6.5 语义核查均为**推论型新结论**（卡面不判项「确实给出前句推不出的新结论的」）→ deny 保留，解说姿态归 FP-3.3（模式B），已记录「转模式建议」。本轮实测首次产出 `fpr-deny:L11` 语义样本 ×7。
- P01 A1 反升（2→3）：**删词修复的副作用实证**——「凳面还是温的,是刚有人离开的余热」删词后句长落入 15 字同桶，与邻句 signature 同构新造窗（定位：修订稿段14）。白名单内无 FP-1.5a finding → 一轮不处理；prose-revision 已写明「修订候选须重跑预筛」，机制兜得住但会多一轮。P01 判定式（逗号数/冒号/括号/15字桶，`signature()`）与卡面 FP-1.5a「成分顺序相同」语义宽窄差见 F-1。
- P03 3 处残留：按卡面改法补回指（「问题在于,谁需要**这**批…」「**这位**管库房的老周头」）后机器仍命中——机器 P03 判定式=「段首 COMMENT 词开头且**段首字符**非回指词」，补句内回指不消段首判定。见 F-1。
- P02×1/P01×2 残留（A3 段10）：该段四句天然 1 逗同桶，句首「可是」**不在 FP-6.4 卡面词表（6词）也不在 L10 正则**——无规则可指认 → 白名单外不改。见 F-5。

### 1.2 A 组语义项（卡面 FP 编号特征，修订 agent 逐条指认；裁-9：记录项非硬线）

| 段 | 埋入特征 → 修订后 | 关键 excerpt（before → 处置） |
|---|---|---|
| A1 | FP-1.1 ×5 → **0**（机器 L01 只抓 4，「与其说他紧张，不如说」为第 5 处，且 L01b 因句中逗号**漏检**→F-6） | 「不是惩罚，而是清零」→「就是清零」；「这不是胆怯，而是敬畏」→「这是敬畏」 |
| A1 | FP-2.1 13 次/851字(15.3/千字) → **4**(4.7/千字，≤5 达标) | 忽然×4/不禁×3 删、宛如→像、仿佛→好像、猛地×1 删 |
| A1 | FP-2.4 缓缓×2 → **0**；「平静地」×3 保留 | 「平静地响起来/平静得/平静地落下来」均非「+说/问/道」词表搭配，把握不足不改 |
| A1 | FP-1.7 揭晓式 3 → **0**（另 1 处非揭晓保留） | 「清零——积分」→冒号；「敬畏——对规则的敬畏」→逗号 |
| A2 | FP-2.3 空洞形容词 10 处 → **0** | 「令人震惊的是，没有一个人开口」→删提示语；「这样的场面令人震惊，这样的沉默不可思议，这样的人心无法形容」→「这样的场面，这样的沉默，这样的人心。」 |
| A2 | FP-3.1 「他想起×3/脑海中浮现」3 组 → **0** | 记忆直接呈现，实词全保留：「父亲在世时也这样站着，父亲教他握笔的手，那年被雨打湿的半卷书，尽是旧事。」 |
| A2 | FP-1.5b 连续 4 段「他+动词」段首 → **0** | 「他跪下来」→「跪下来」（段首藏主语，打断连续） |
| A2 | FP-1.5a「他等着」相邻句同构 → **0**（机器 P01 1→0） | 「他等着有人回应。他等着母亲的眼泪…」→「他等着有人回应：母亲的眼泪…」 |
| A2 | FP-3.4 「他」≈44 → 31（32.6/千字，C 级锚点 15/千字**仍超**） | 最小改动下删冗余主语 13 处（含承前省）；全清需段级主语重构=模式B 级动作量，残留为结构性，转模式建议 |
| A3 | FP-6.2 ×1 → **0** | 「当第二次查库时，」→「第二次查库，」 |
| A3 | FP-6.4 句首连接词 9 处（然而4/因此2/此外2/总而言之1）→ **0**（机器 L10 before=0：正则只认「段首+后跟逗号」形态，句首/无逗号形态全漏→F-6） | 「然而她没有声张。因此她把卷宗放回原处」→「她没有声张，把卷宗放回原处」 |
| A3 | FP-6.5 真复述 2 → **0**；推论型 6 → deny 保留 | 「这意味着有人换过封条，却没来得及换纸」（前句同义整合）→删提示语；「换句话说，她现在是在跟一棵根深的老树较劲」（「有根基」的比喻复述）→删提示语 |
| A3 | FP-3.5 真命中 2 → 补回指修 2；机器口径下另有 2 处本不命中（「听起来，**这**像是」「换句话说，**她**」句内有回指） | 卡面排除项「已有回指成分」生效 |
| A3 | FP-3.3 旁白解说（不难看出×2/由此可见/值得注意×2/关键在于）→ **不改**（模式B 白名单外，记转模式建议） | 双模式边界按设计生效——模式A 不越界清旁白 |

信息守恒抽查（每段 3 处）：A1「清零/灯塔/敬畏」、A2「真相/眼泪/酸楚·恨意」、A3「换封条·没来得及换纸/第二次查库/老周头」——改后实词全部可溯源；零新增实词（「这位/这批」为指示词，非新信息）；对话与引语零改动（行 diff 复核：A1 改 6/17 段、A2 11/17、A3 9/13，其余段逐字保留）。

### 1.3 B 组人类安全测试（硬验收）

| ID | screen 命中(before) | 处置 | 改动数 | diff |
|---|---|---|---|---|
| FIX-R1-B1 (A055) | **P03×1**：「看上去很大度, 其实他忍得咬碎了牙, 巴不得我晾江越一辈子。」 | 卡面 FP-3.5 语义核查：整句含回指「他」→命中排除项「已有回指成分」→ **finding 不成立，deny**（`fpr-deny:P03` 语义样本）；机器命中因判定式只查段首（F-1） | **0** | **空** |
| FIX-R1-B2 (A020) | 0（仅 measure M03×4/M05×8） | 白名单零改动 | **0** | **空** |
| FIX-R1-B3 (A005) | 0（仅 measure M03×1/M04×5/M05×14） | 白名单零改动 | **0** | **空** |

**判据 2 通过（0% 改动 ≤2%）**；B1 的 P03 是 F-1 口径差在人类文本上的直接误报实锤，同时演示了「screen 候选注入审查 → 卡面语义 deny → 零改动」的完整合规路径。

### 1.4 双向判据结论

- 判据 1（A 组指纹下降）：screen 合计 −54%；6 类埋入规则中 4 类清零（L01/FP-2.3/FP-3.1/FP-6.2/6.4/1.5a/1.5b）、1 类显著下降（L03 9→2）、1 类半降（L11 9→7，残留全部 deny 有因）。语义项（FP-2.x/3.x）除 FP-3.4 结构性残留外全部清零（记录项，方向性达标）。
- 判据 2（B 组零改动/极小）：**3/3 段零改动、diff 全空**，且产生 1 条有价值的误报证据（P03）。

---

## 2. 对抗 diff 审查 findings（P0/P1/P2）

### F-1【P1】P03/P01 判定式与卡面语义不同口径，「同口径」声称不实（P1-6 修复只对齐词表、未对齐判定式）
- 卡面 FP-3.5 =「段首以评论语开头且**整句无回指成分**」；脚本 P03（`runParagraphRules`）= `COMMENT.test(paraText) && !ANAPHOR.test(paraText)`，两个正则均 `^` 锚——**实义为「段首是评论语且段首字符不是回指词」**，句内回指（「这/他/她」）完全不豁免。实测双重实锤：B1 人类段误报（整句含「他」仍命中）；A3 补回指修订后 3 处残留（按卡面语义已合规，机器永不消）→ 修订 agent 按卡面改法将陷入「同因复发」循环直到 G6 升级。
- 同病：映射表 FP-1.5a→P01 行无口径注记（FP-1.5b→P02 行反而有「口径差已登记」），机器 signature（逗号数/冒号/括号/15字桶）不查「成分顺序」，且 A1 出现修复致 P01 反升的实证。
- 处置建议（二选一）：① 映射表 FP-3.5/FP-1.5a 行补「判定式口径差已登记」注记（FP-1.5b 同款），并把 FP-3.5 的卡面措辞从「整句无回指」改为「段首非回指词开头（以脚本判定式为准）」；② R2 改 P03 判定式为「段首句 COMMENT + 该句查 ANAPHOR」。①零代码、本轮可落。
- 依据：裁-8 与 D1 §8 接口 2「脚本与判级必须同口径，否则基线与判级漂移」——本卡自立的硬要求。

### F-2【P1】「零容忍型」标签的证据跨口径（FP-1.5b）
- 零容忍依据 = P02 金丝雀基线 0（**段内**连续 3 句同构口径），卡面 FP-1.5b = **段间**「连续 3 段以他+动词开头」——基线证据测的不是卡面说的现象，零容忍判级的实证支撑跨了口径（口径差已登记是诚实的，但标签强度应打折）。建议映射表该行补「零容忍依据仅覆盖段内口径；段间口径无基线，判级按卡面+人工」。

### F-3【P2】ANAPHOR 词数转述错误：卡面写「28 词」，脚本实为 **27** 项
- 逐项清点（这/那/其/此/上面/前面/刚才/以上/该/它/他/她/它们/他们/同样/类似/相比/反过来/但/不过/所以/因此/于是/而/另/除此/与此）= 27。COMMENT 20 词正确。卡自称「逐字权威」照录，计数错 1 恰是「转述不精确」的反面教材；改为「27 词」或删计数只留指向。

### F-4【P2】「禁裸数字入库」与触发标记结构量词的界线未声明
- FP-6.1「超十五字」「『的』字连续两个以上」、FP-1.6「≥2 个顿号连起 ≥3 项」等规则定义内数字无锚点标注。可辩护为「触发标记构成而非判级阈值」，但 §0 的禁裸数字纪律字面覆盖所有新增数字，G4 按字面执行会误伤。建议 §0 补一句「触发标记内的结构量词（如『超十五字』『≥3 项』）属规则定义免标注；判级阈值一律标锚点」。

### F-5【P2】FP-6.4 卡面词表（6词）与叙事高频词的覆盖缺口
- 「可是/但是/所以」等叙事高频转折词不在卡面词表也不在 L10 正则；A3 段10 残留 P01×2/P02×1 的主因即「可是」开头的 1 逗同构段无规则可指认。属规则扩充事项，应走编号追加纪律 + 金丝雀校验，本轮不算错；登记待办即可。

### F-6【P2】机器正则的标点敏感漏检（L01b/L10）
- 「与其说他紧张，不如说他亢奋」：L01b 要求「与其说」后 20 字内无逗号直连「不如说」——句中逗号致漏检（卡面变体第 5 处机器全漏）；L10 只认「段首+连接词后紧跟逗号」，「然而她没有声张」「总而言之，」段中句首形态全漏（A3 卡面命中 9 处、机器 0）。机器覆盖缺口使 deny 率口径（R2）将含大量卡面命中/机器未报的差异——建议在 R2 规则表扩项时登记，本轮记录。

### F-7【P2】fixtures B 组「字数」栏与实测差 ~11%，复现口径未写统计式
- 按登记段落范围切出（段数 841/724/689 **完全吻合**，选段可复现），实测去空白字符 802/796/813 vs 登记 902/906/911；含换行 860/852/863 亦不合。判据 2 的 diff 断言不受影响，但「长度配对 ±20%」的声明应按可复现统计口径重写（建议登记统计式：去空白 code point 数）。

### F-8【P2】slotReviewFeedback 仍丢弃 findings[].code——P1-3 以「message 写编号」方案闭环，但机器无强制
- 实测：`--review-feedback` 注入后回执区渲染 `[warning] [fpr:L01] 不是惩罚,而是清零（证据: [para3]）`——**message 头部编号到达修订方，闭环成立**（P1-3 处置方案①按推荐落地）；`code` 字段本身仍被丢弃，若审查 agent 违反 rubric 的「message 头部必须携带编号」纪律则编号断流，兜底 = rubric「指认不出编号自动降 note」。机器级强制（透传 code）留 R2 schema 合并轮加固。

### F-9【P2】注入体积增量记账（P2-18 的数字补全）
- fingerprint 卡 7,077 → 27,172 字节（+20,095）；prose-revision 卡 1,907 → 9,585（+7,678）。prose-review 组装产物实测 69,433 字节。一次性注入可接受，但七卡 craft_refs 总预算影响应记入 metrics（M5）。

### 规格符合性核对（无 finding 项）
- **composer +1 行**：确证，仅 ASSET_DIRS 增 `'prose-revision'` 一行；键名 kebab-case 与相邻键一致；`--asset` 枚举已含 prose-revision。
- **映射表 21 行逐行对照**：21 行与脚本 43 条注册表逐行核对**无错配、无未知 ID**；FP-1.2 墓碑行格式规范（retired/永不复用）；FP↔fpr 对应（L01/P01/P02/L02/L03/L06/P03/L07a/L08/L09/L10/L11）全对；判级语义分档 **零容忍 3（L06/L11/P02）/ 阈值 7（L01/L02/L03/L07a/L08/P01/P03）/ 观察期 2（L09/L10）与 U1 方案 A 完全一致**；rubric 增补段中的对应关系亦全对。唯一保留意见见 F-2。
- **「不作为判级理由」12 行**：行行有实测依据（N1-N10 带 lieflat 倍率；N8 已按 P2-11 拆分引用 0.05× 与 1.03× 双数据；N11/N12 为本仓适配/元规则行）；行行有叙事适配注记（N1-N10 行内均有，N11 整行即适配）；P2-10（1.67 数学错误）已随「数值指向 hardrules、不复述数字」消除；P2-12 已改「≤0.09/千字（部分为 0.00）」。
- **rubric 增补段**：纯增行（+12/−0）；判级词反引号体例与既有 rubric 一致；编号写入 message 格式（`[fpr:L03]`/`[FP-2.1]`）与 prose-revision 消费格式**双向闭环**（本复审实测组装产物验证）。
- **双模式自洽**：模式A 编号白名单（FP-1.x 除墓碑/FP-2.x/FP-3.1/3.2/3.4/3.5/FP-6.x+硬规则）与映射表「修订模式归属」列逐条一致（P1-4 落地）；T×Canon 用例组 U1-U4 判据（一次性/不回收/无记账价值 vs 可记账）可操作，能裁决「跑题细节 vs 新增事实」（P1-9 落地）。**模式B Canon 反例推演（FIX-R1-A2 场景）**：注入「他攥紧的袖口上,还留着去年离家时母亲缝的暗记」——暗记=新物件+新往事，命中 U2+U4 双违规判「事实，回滚」✓；「母亲的鬓发还是黑的」（既有事实感官面）判「质感」✓——用例组在该场景下裁决有效。
- **红线**：卡内 lieflat 倍率全部处于〔方向佐证〕语境并声明「不可直比、不充当阈值」（FP-1.6 判级上限 warning 待 g2 校验）——无倍率当阈值；无不可核验的新判级数字（结构量词的界线含混见 F-4）；豁免三件套 + 整体援引禁令 + 方法论硬约束不可豁免，总豁免后门封死。
- **P1 落地证据**：P1-3 ✓（F-8）；P1-4 ✓（编号白名单）；P1-5 ✓（FP-2.1-2.5 行级编号/FP-1.7 编号/FP-1.2 墓碑说明）；P1-6 半落地（词表对齐 ✓，判定式未对齐 → 升 F-1）；P1-7 ✓（RT 系判官人工计数留档+实跑累积写入 §4 校验口径）；P1-8 ✓（fixtures 头部：判官指认+主控复核/记录项非硬线/D5 §2.5 单一权威）；P1-9 ✓（用例组）。P2-11/12/14/15/16/17 ✓ 全落。

---

## 3. 结论

**修改后放行。** 盲测双向判据实测达标（A 组 screen −54%、语义项主类清零；B 组 3/3 零改动 diff 空），四命令全绿（guardrails 245/0、fingerprint 49/0、canary --compare PASS、compose prose-revision exit 0 且 prose-review 组装 69,433B 中新表关键词全中——craft_refs 逐字注入传导成立）。本次修订最大的收获是两条**只有真跑才能拿到的口径差实证**（F-1 的 P03 判定式、F-6 的正则标点漏检），它们正是裁-8「脚本与判级同口径」要防的漂移。

**必改项（放行前置，均为零代码文本修改）**：
1. 【F-1】映射表 FP-3.5 行补「判定式口径差」注记并把卡面措辞改为以脚本判定式为准（或登记 R2 改 P03 判定式）；FP-1.5a 行补同款注记。
2. 【F-2】FP-1.5b「零容忍型」行补「依据仅覆盖段内口径」限定。

**随批顺手修（P2）**：F-3（27 词）、F-4（结构量词免标注口径）、F-7（fixtures 字数统计式）。

**登记待办（不改本轮产物）**：F-5（FP-6.4 词表扩充走追加纪律）、F-6（L01b/L10 正则覆盖缺口 → R2 规则表扩项）、F-8（code 透传 → R2 schema 合并轮）、F-9（M5 记账）。

> 附注：本复审 A 组修订由红方亲自执行（同一实体兼修订与复核），机器计分为客观第三方；语义项指认已按 P1-8 处置逐条给出 excerpt 供主控复核计数，未采用模型口算总数。
